export const metadata = {
  title: "Contact | TrivoxAds",
  description:
    "Get in touch with TrivoxAds – address, phone, email, Instagram, and Facebook. Send a message via WhatsApp or reach out directly.",
};

export default function ContactLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
